v16 上传 GitHub：

1. 建议先在旧网页下载一次 JSON 备份。
2. 删除 GitHub 仓库旧的 index.html 和整个 assets/。
3. 上传本包中的 index.html、assets/、.nojekyll、supabase/ 等到仓库根目录。
4. 不需要重新运行 Supabase SQL，也不需要重新注册账号。
5. GitHub Pages 部署后 Ctrl+F5 强制刷新。

注意：v16 已移除记账与攒钱模块。新版首次正常保存到云端后，这两个旧字段将不再包含在云端 state 中。
